<h2>
    Inbox (<?= $_SESSION["pagination"]["total_counter"] ?>)
</h2>

<div class="mail-tools tooltip-demo m-t-md">
    <?= $pagination ?>

    <button class="btn btn-white btn-sm" data-toggle="tooltip" data-placement="left" title="Refresh inbox"><i class="fa fa-refresh"></i> Refresh</button>
    <button class="btn btn-white btn-sm" data-toggle="tooltip" id="markAsRead" data-placement="top" title="Mark as read"><i class="fa fa-eye"></i> </button>
    <button class="btn btn-white btn-sm" data-toggle="tooltip" data-placement="top" title="Mark as important"><i class="fa fa-exclamation"></i> </button>
    <button id="sendToArchive" class="btn btn-white btn-sm" data-toggle="tooltip" data-placement="top" title="Move to trash"><i class="fa fa-trash-o"></i> </button>

</div>

<div class="mail-box">
    <table class="table table-hover table-mail">
        <tbody>
            <?php foreach ($arrMessages as $arrMessage):
                if ( $arrMessage['has_read'] == 1 )
                {
                    $class = "read";
                }
                else
                {
                    $class = "unread";
                }
                ?>

                <tr class="<?= $class ?>">
                    <td class="check-mail">
                        <input id="<?= $arrMessage['id'] ?>" type="checkbox" class="i-checks checkbox">
                    </td>

                    <td class="mail-ontact"><a class="job" jobid="<?= $arrMessage['id'] ?>" href="#"><?= $arrMessage['customer_firstName'] . " " . $arrMessage['customer_lastName'] ?></a></td>  
                    <td class="mail-subject"><a class="job" jobid="<?= $arrMessage['id'] ?>" href="#"><?= $arrMessage['comment'] ?></a></td>
                    <td class=""><?= $arrMessage['description'] ?></td>
                    <td class=""><?= $arrMessage['returnId'] ?></td>
                    <td class="text-right mail-date"><?= $arrMessage['date_added'] ?></td>
                </tr>


<?php endforeach; ?>
        </tbody>
    </table>
</div>